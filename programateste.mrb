# Get a range.puts'Escolha dois numeros: ';min = gets.to_i;max = gets.to_i;# Invert them if necessary.min, max = max, min ifmin > max;# Compute the sum.sum = 0;fore inmin..max dosum = sum + e;end# Get how many chances?puts'Calculei o somatorio da progressão de ' + min.to_s+ ' ate ' + max.to_s+ '.';print'Com quantas chances você quer tentar acertar esse valor? ';chances = gets.to_i;# Attempts.n = 0;found = 0;attempts = [];whilen < chances andfound != 1 don = n + 1;print'  Chance ' + n.to_s+ ' de ' + chances.to_s+ ': ';x = gets.to_i;ifx === attempts thenputs'Voce ja tentou o valor ' + x.to_s;n = n -1;elseifx < sumputs 'O valor e maior que ' + x.to_s;elsifx > sumputs 'O valor e menor que ' + x.to_s;elseputs 'Parabens, voce acertou.';found = 1;endattempts = attempts + [x];endend# Print loser.puts'Voce nao conseguiu. O valor correto era ' + sum.to_s+ '.' iffound != 1;# Print all attempts.i = 0;whilei < attempts.lengthdoifi == 0 thenprint'  Voce tentou os seguintes valores: ';elseprint', ';endprintattempts[i].to_s;i = i + 1;endputs;